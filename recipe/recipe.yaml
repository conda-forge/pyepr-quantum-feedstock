schema_version: 1

context:
  version: "0.9.6"

package:
  name: pyepr-quantum
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/p/pyepr-quantum/pyepr_quantum-${{ version }}.tar.gz
  sha256: 9310726f53a04dacd19f9014f60ea04cbc40a2b1f0dfb925e364f3004659f92b

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=61
    - wheel
    - pip
  run:
    - python >=${{ python_min }}
    - addict
    - ipython >=7.0.0
    - matplotlib-base >=3.1.0
    - numpy >=1.15.0
    - pandas >=1.0.1
    - pint >=0.8
    - qutip >=5.0.0
    - scipy >=1.3.0
    - seaborn >=0.10.0
    - sympy >=1.3
    - xarray >=0.10.0

tests:
  - python:
      imports:
        - pyEPR
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  summary: Python Energy-Participation-Ratio (EPR) package for quantum chip design
  license: BSD-3-Clause
  license_file: LICENSE
  homepage: https://github.com/zlatko-minev/pyEPR
  documentation: https://pyepr-docs.readthedocs.io/en/latest/

extra:
  recipe-maintainers:
    - wshanks
    - zlatko-minev
